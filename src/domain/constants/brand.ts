/**
 * The product name. Kept out of the locale files on purpose: translators must
 * never rewrite it, so strings that mention the brand interpolate {{brand}}
 * instead of spelling it out.
 */
export const BRAND_NAME = "Lawpantruck"
