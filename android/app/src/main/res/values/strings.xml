<resources>
    <string name="app_name">Lawapan Truck</string>
    <!-- GOOGLE_MAPS_API_KEY is injected at build time from .env by
         react-native-config (resValue in dotenv.gradle). Do not define it
         here — a literal entry self-references and breaks the release lint. -->
</resources>